# Docker Compose Environment Configuration
# Copy to .env and customize for your environment

# ==========================================
# API Service Configuration
# ==========================================
# Image reference for the API service
# For development (local build): aspireapp-apiservice:latest
# For production (Docker Hub): pngan/aspireapp-apiservice:latest or specific version tag
APISERVICE_IMAGE=pngan/aspireapp-apiservice:latest

# Internal port where API service listens
APISERVICE_PORT=8080

# ==========================================
# Web Frontend Configuration
# ==========================================
# Image reference for the web frontend
# For development (local build): aspireapp-webfrontend:latest
# For production (Docker Hub): pngan/aspireapp-webfrontend:latest or specific version tag
WEBFRONTEND_IMAGE=pngan/aspireapp-webfrontend:latest

# Internal port where web frontend listens
WEBFRONTEND_PORT=8080

# Host port where webfrontend is accessible from outside the container
# This is the port exposed to the reverse proxy or direct access
# Common values: 8080 (development), 8080 (production on internal network)
WEBFRONTEND_HOST_PORT=8080

# ==========================================
# Notes
# ==========================================
# 1. For local development: Use docker-compose.yaml with local builds
#    APISERVICE_IMAGE=aspireapp-apiservice:latest
#    WEBFRONTEND_IMAGE=aspireapp-webfrontend:latest
#
# 2. For production deployment: Use docker-compose.production.yaml with Docker Hub images
#    APISERVICE_IMAGE=pngan/aspireapp-apiservice:v1.0.0  (or :latest)
#    WEBFRONTEND_IMAGE=pngan/aspireapp-webfrontend:v1.0.0  (or :latest)
#
# 3. Services are isolated on internal Docker network except:
#    - webfrontend: Exposed on WEBFRONTEND_HOST_PORT to the host
#    - apiservice: Only accessible to webfrontend via Docker DNS
#
# 4. Health checks monitor both services at /{path}/health endpoints
#    webfrontend will not start until apiservice is healthy
#
# 5. Aspire Dashboard (optional telemetry/monitoring) runs on port 18888
